# --- Node / React ---
node_modules/
frontend/node_modules/

# --- Python ---
__pycache__/
*.pyc
.venv/

# --- Runtime data (form submissions live here) ---
backend/eshodha.db
backend/eshodha.db-*

# NOTE: backend/static (the built React app) IS committed so the site runs
# with Python alone. Rebuild it with: cd frontend && npm install && npm run build
